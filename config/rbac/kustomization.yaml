resources:
- service_account.yaml
- role.yaml
- role_binding.yaml
- leader_election_role.yaml
- leader_election_role_binding.yaml
# These resources protect the secure controller-runtime metrics endpoint.
# Bind metrics_reader_role.yaml to the Prometheus service account used by the
# cluster when Prometheus is configured to scrape this operator.
- metrics_auth_role.yaml
- metrics_auth_role_binding.yaml
- metrics_reader_role.yaml
